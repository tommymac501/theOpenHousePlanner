services:
  - type: web
    name: open-house-planner
    env: node
    buildCommand: ./build.sh
    startCommand: node start-production.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_PRODUCTION_DEPLOYMENT
        value: "true"
